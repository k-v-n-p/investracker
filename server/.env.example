# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/investracker?retryWrites=true&w=majority

# Random secret for JWT signing (openssl rand -hex 32)
JWT_SECRET=change-me-to-a-long-random-string

# SHA-256 hash of your dashboard login password
# Generate in browser console:
# crypto.subtle.digest('SHA-256', new TextEncoder().encode('YourPassword'))
#   .then(b => console.log([...new Uint8Array(b)].map(x => x.toString(16).padStart(2,'0')).join('')))
AUTH_PASSWORD_HASH=5e884898da28047151d0e56f8dc6292773603d0d6aabbdd62a11ef721d1542d8

# GitHub Pages origin (comma-separated for multiple)
CORS_ORIGIN=https://k-v-n-p.github.io

# Optional — defaults to "owner"
USER_ID=owner

PORT=3000
